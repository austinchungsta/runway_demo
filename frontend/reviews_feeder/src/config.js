/*
Small config file to store endpoints and constants
*/

const API_BASE_URL = 'http://127.0.0.1:5000'; // Chage to whatever port your backend is running on

export const API_ENDPOINTS = {
    GET_REVIEWS: `${API_BASE_URL}/get_reviews`
};